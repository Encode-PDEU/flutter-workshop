void main() {
    const PI = 3.14; // const -> Value is initialized and fixed at compile-time
    final rollNo = 100; // final -> Value is initialized and fixed at runtime
    
    print(PI);
    print(rollNo);
}
